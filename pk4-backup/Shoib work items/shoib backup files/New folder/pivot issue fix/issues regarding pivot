	//this code is causing pivot not to work at all
	document.getElementsByClassName = function(){
			if(document.hasChildNodes && arguments[0]){
				var data = new Array();
				for(a=0;a<document.getElementsByTagName("*").length;a++){
					if(document.getElementsByTagName("*")[a].className == arguments[0]){
					data.push(document.getElementsByTagName("*")[a]);
					}
				}
			return data;
			}
		}


		//this block is causing bad ui for sortable, droppable and draggable
		pageload() //function
		else if(L_historyGrp == "subMenu" ) //in this block , the ui for draggbale is not proper
		{
			$.getJSON(JSONURL,'',function(json){setupMainMenuUrlHistory(json,hash);});
			setupMainMenuUrlHistory();//this function is in jqueryhistorynew.js
		}


//place this script after the cjquery script
 <script type="text/javascript" src="http://ajax.googleapis.com/ajax/libs/jqueryui/1/jquery-ui.min.js"></script>


//you have commented pivot htmlpage code, uncomment it to use it while load

